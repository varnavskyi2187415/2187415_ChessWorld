import React, { memo } from 'react';


const Home: React.FC = () => {
  return (<>
    <div className="container">

      text
      <button>
        Find game
      </button>
    </div>
  </>)
}

export default memo(Home);